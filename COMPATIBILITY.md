# 兼容性

## 运行环境

| 能力 | 最低/支持范围 | 说明 |
| --- | --- | --- |
| Skill 文档 | 支持 Agent Skills 渐进披露和相对链接的运行器 | `name`、`description` 为基础字段；根级另含标准可选元数据 |
| 公共脚本 | Python 3.9 或更高版本，Python 标准库 | 不需要第三方 Python 包，不写入项目目录 |
| 文件系统 | 可读取 UTF-8 文本和 JSON 的常见桌面/CI 环境 | shell 示例采用可替换路径，不依赖特定用户名 |

## 小程序工程

| 类型 | 支持方式 | 限制 |
| --- | --- | --- |
| 原生微信小程序 | 识别 `app.json`、`project.config.json`、分包和既有测试能力 | `miniprogram-simulate`、mpflow 仅在项目已具备时作为可选能力；不能替代真机 |
| Taro | 识别依赖和既有 weapp 脚本，尊重项目真实输出目录 | 不自动安装、不套用原生目录假设、不绑定具体 Taro 版本 |
| uni-app | 识别依赖、`manifest.json`、`pages.json` 和 mp-weixin 脚本 | 区分 DCloud 标识与微信 AppID；不从配置存在推导平台通过 |
| 其他/混合框架 | 报告 `unknown` 或 `ambiguous` 后人工确认 | 不猜测框架、不执行候选命令、不自动迁移 |

## 证据层限制

- 静态、单元、集成、模拟器、真机、云端、体验版、审核版和正式版分别取证。
- DevTools CLI、设备、账号和平台后台均为可选外部能力；未提供时保持未执行或 `unknown`。
- 套件不自动安装依赖，不自动修改项目，不自动上传、审核、发布或创建云资源。
